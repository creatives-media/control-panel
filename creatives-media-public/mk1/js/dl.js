function dlManager() {
    let protocol = 'https://';
    let host = 'public.creatives-media.ghiveci.com/';
    let structure = 'dl/';
    let dlLocation = protocol+host+structure+dlID;

    window.open(dlLocation, "_blank");
}